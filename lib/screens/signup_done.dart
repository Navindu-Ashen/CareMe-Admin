import 'package:flutter/material.dart';

class SignupDoneScreen extends StatelessWidget {
  const SignupDoneScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Sign up Done"),
      ),
    );
  }
}
